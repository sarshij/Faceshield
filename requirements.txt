# ──────────────────────────────────────────────
# FaceShield — Python Dependencies
# ──────────────────────────────────────────────
# Install:  pip install -r requirements.txt
# GPU note: If CUDA is available, MediaPipe and OpenCV
#           will auto-utilise GPU acceleration.
# ──────────────────────────────────────────────

fastapi==0.115.6
uvicorn[standard]==0.32.1
python-multipart==0.0.18
aiofiles

# Computer vision & ML
opencv-python==4.10.0.84
mediapipe==0.10.20
numpy==1.26.4
Pillow==11.0.0
scipy==1.14.1

# Face tracking
deep-sort-realtime==1.3.2
torch
torchvision

# Video encoding helpers
ffmpeg-python==0.2.0

# Optional — better face embeddings (uncomment if needed):
# insightface==0.7.3
# onnxruntime-gpu==1.20.0
